1、里氏转换
1)、子类可以赋值给父类
2）、如果父类中装的是子类对象，那么可以将这个父类强制转换成子类对象。

2、
子类对象可以调用父类中的成员，但是父类对象永远只能调用自己的成员。

3、
is：表示类型转化，如果能够转换成功，则返回一个true，否则返回一个false
as：表示类型转化，如果能够转换，则返回对应的对象，否则返回一个null

4、protected
可以在当前类的内部以及该类的子类中访问

5、ArrayList集合长度问题
每次集合中实际包含的元素的个数（count）超过了可以包含的元素的个数（capcity）的时候，集合就会像内存中申请多开辟一倍的空间，来保证集合的长度一直够用

6、Hastable 键值对集合
在键值对集合中，是根据键去找值
键值对对象[键]=值;
键值对集合当中，键必须是唯一的，值是可以重复的

7、foreach循环
